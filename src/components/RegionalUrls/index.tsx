/**
 * Kept so existing MDX imports stay valid.
 * Singapore / Mumbai now live on the language sample card.
 * See src/region/region-sync.js.
 */
export default function RegionalUrls(_props?: {
  path?: string;
  host?: 'api' | 'ccm';
}): null {
  return null;
}
