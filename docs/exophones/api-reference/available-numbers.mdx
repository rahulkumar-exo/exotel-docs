---
id: available-numbers
title: Available ExoPhones
sidebar_label: Available Numbers
---
import ApiConsole from '@site/src/components/ApiConsole';
import TryItBanner from '@site/src/components/TryItBanner';

# Get Available ExoPhones

Search for available phone numbers by country and type that can be purchased for your account.

<TryItBanner method="GET" endpoint="/v2_beta/Accounts/{sid}/AvailablePhoneNumbers/{country}/{type}" />

## HTTP Request

```
GET /v2_beta/Accounts/<account_sid>/AvailablePhoneNumbers/<iso_country_code>/<number_type>
```

## Path Parameters

| Parameter | Description |
|-----------|-------------|
| `iso_country_code` | Two-letter country code (e.g., `IN`, `MY`, `SG`) |
| `number_type` | `Landline`, `Mobile`, or `TollFree` |

## Query Parameters

| Parameter | Required | Description |
|-----------|----------|-------------|
| `IncomingSMS` | No | Filter numbers that support incoming SMS |
| `InRegion` | No | Filter by telecom circle (e.g., `IN`, `AP`, `DL`, `KA`) |
| `Contains` | No | Filter by substring match in the number |

## Response

```json
[
  {
    "friendly_name": "+60XXXXXXX1",
    "phone_number": "+60XXXXXXXX1",
    "capabilities": {
      "sms": false,
      "voice": true
    },
    "country": "MY",
    "region": "MY",
    "number_type": "Landline",
    "rental_price": "0.000000"
  }
]
```

### Response Fields

| Field | Description |
|-------|-------------|
| `friendly_name` | Human-readable phone number |
| `phone_number` | Full phone number in E.164 format |
| `capabilities` | Object with `sms` and `voice` boolean flags |
| `country` | Country code |
| `region` | Telecom region/circle |
| `number_type` | Type of number |
| `rental_price` | Monthly rental price |

## Try It

<ApiConsole
  method="GET"
  path="/v2_beta/Accounts/{account_sid}/AvailablePhoneNumbers/{iso_country_code}/{number_type}"
  params={[
    { name: 'iso_country_code', required: true, type: 'string', description: 'Two-letter country code (replaces {iso_country_code} in URL)', placeholder: 'IN' },
    { name: 'number_type', required: true, type: 'select', description: 'Type of phone number (replaces {number_type} in URL)', options: ['Landline', 'Mobile', 'TollFree'] },
    { name: 'InRegion', required: false, type: 'string', description: 'Filter by telecom circle', placeholder: 'DL' },
    { name: 'Contains', required: false, type: 'string', description: 'Filter by substring in the number', placeholder: '9876' },
  ]}
/>
