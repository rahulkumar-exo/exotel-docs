---
id: number-metadata
title: Number Metadata
description: Use the Exotel API to retrieve telecom metadata for Indian phone numbers, including operator, telecom circle, number type, and DND status.
sidebar_label: Number Metadata
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import ApiConsole from '@site/src/components/ApiConsole';
import TryItBanner from '@site/src/components/TryItBanner';
import RegionalUrls from '@site/src/components/RegionalUrls';

# Number Metadata

Retrieve telecom information about Indian phone numbers, including the telecom circle, operator, number type, and DND (Do Not Disturb) status.

<TryItBanner method="GET" endpoint="/v1/Accounts/{sid}/Numbers/{phone_number}.json" />

<RegionalUrls path="/v1/Accounts/{sid}/Numbers/{phone_number}.json" host="api" />

Append `.json` for JSON response format.

## Path Parameters

| Parameter | Required | Description |
|-----------|----------|-------------|
| `phone_number` | Yes | The Indian phone number to look up |

## Code Examples

<Tabs groupId="programming-language">
<TabItem value="curl" label="cURL">

```bash
curl "https://<your_api_key>:<your_api_token>@api.exotel.com/v1/Accounts/<your_sid>/Numbers/09876543210.json"
```

</TabItem>
<TabItem value="python" label="Python">

```python
import requests

response = requests.get(
    'https://<your_api_key>:<your_api_token>@api.exotel.com/v1/Accounts/<your_sid>/Numbers/09876543210.json'
)

print(response.json())
```

</TabItem>
<TabItem value="nodejs" label="Node.js">

```javascript
const apiKey = '<your_api_key>';
const apiToken = '<your_api_token>';
const accountSid = '<your_sid>';

const url = `https://api.exotel.com/v1/Accounts/${accountSid}/Numbers/09876543210.json`;

const response = await fetch(url, {
  headers: {
    'Authorization': 'Basic ' + Buffer.from(`${apiKey}:${apiToken}`).toString('base64'),
  },
});

const data = await response.json();
console.log(data);
```

</TabItem>
</Tabs>

## Response

```json
{
  "Numbers": {
    "PhoneNumber": "0XXXXX30240",
    "Circle": "GJ",
    "CircleName": "Gujarat Telecom Circle",
    "Type": "Mobile",
    "Operator": "R",
    "OperatorName": "Reliance",
    "DND": "Yes"
  }
}
```

## Response Fields

| Field | Type | Description |
|-------|------|-------------|
| `PhoneNumber` | String | Reformatted phone number |
| `Circle` | String | Two-character telecom circle code |
| `CircleName` | String | Human-readable circle name |
| `Type` | String | `Mobile` or `Landline` |
| `Operator` | String | Telecom operator code |
| `OperatorName` | String | Full operator name |
| `DND` | String | `Yes`, `No`, or `Unavailable` |

## Telecom Circles (India)

| Code | Circle Name |
|------|-------------|
| AP | Andhra Pradesh |
| AS | Assam |
| BR | Bihar |
| CH | Chennai |
| DL | Delhi |
| GJ | Gujarat |
| HP | Himachal Pradesh |
| HR | Haryana |
| JK | Jammu & Kashmir |
| KA | Karnataka |
| KL | Kerala |
| KO | Kolkata |
| MH | Maharashtra |
| MP | Madhya Pradesh |
| MU | Mumbai |
| NE | North East |
| OR | Orissa |
| PB | Punjab |
| RJ | Rajasthan |
| TN | Tamil Nadu |
| UE | UP (East) |
| UW | UP (West) |
| WB | West Bengal |

## Telecom Operators (India)

| Code | Operator Name |
|------|---------------|
| AC | Aircel |
| A  | Airtel |
| AL | Allianz |
| B  | BSNL |
| D  | Dishnet |
| E  | Etisalat |
| H  | HFCL |
| I  | Idea |
| LO | Loop |
| MT | MTNL |
| P  | Ping |
| R  | Reliance |
| S  | Sistema |
| ST | S Tel |
| T  | Tata |
| U  | Unitech |
| VI | Videocon |
| V  | Vodafone |

:::note
This API is currently available for **Indian phone numbers only**.
:::

## Try It

<ApiConsole
  method="GET"
  path="/v1/Accounts/{account_sid}/Numbers/{phone_number}.json"
  params={[
    { name: 'phone_number', required: true, type: 'string', description: 'Indian phone number to look up', placeholder: '09876543210' },
  ]}
/>
