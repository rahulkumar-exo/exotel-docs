---
id: connect-voice-ai
title: Connect Voice AI API
description: Dial a number and connect the answered call directly to your bot over AgentStream WebSocket.
sidebar_label: Connect Voice AI
slug: /agentstream/connect-voice-ai
sidebar_position: 4
---

import ApiConsole from '@site/src/components/ApiConsole';
import TryItBanner from '@site/src/components/TryItBanner';
import RegionalUrls from '@site/src/components/RegionalUrls';

# Connect Voice AI API

Dial a phone number and connect the answered call to your conversational AI bot over a bidirectional WebSocket. No App Bazaar flow required.

Typical use cases: outbound campaigns, virtual agents, surveys, and appointment reminders.

<TryItBanner method="POST" endpoint="/v1/Accounts/{AccountSid}/Calls/connect" />

<RegionalUrls path="/v1/Accounts/{AccountSid}/Calls/connect" host="api" />

## When to use

- Bot is the whole experience (campaigns, surveys, reminders)
- Fixed `StreamUrl` in the request is fine
- No IVR, compliance applet, or agent transfer in Exotel

For multi-step journeys, use [Connect with Flow](./connect-voice-ai-flow). For per-call dynamic control, use [Programmable Voice](./programmable-voice).

## Authentication

HTTP Basic Auth — API Key as username, API Token as password ([Dashboard → API credentials](https://my.exotel.com)).

**Base URL:** `https://api.in.exotel.com` (India) · `https://api.exotel.com` (Singapore)

## Request parameters

### Required

| Parameter | Type | Description |
|-----------|------|-------------|
| `From` | String | Number to dial. E.164 (e.g. `+919876543210`) |
| `CallerId` | String | Your ExoPhone / virtual number (caller ID) |
| `StreamUrl` | String | Bot WebSocket URL (`ws://` or `wss://`), max 600 chars |
| `StreamType` | String | Set to `bidirectional` for two-way audio |

### Optional

| Parameter | Type | Description |
|-----------|------|-------------|
| `Record` | Boolean | `true` to record the call. Default: `false` |
| `RecordingChannels` | String | `single` (merged) or `dual`. Default: `single` |
| `TimeLimit` | Integer | Max call duration in seconds. Max: `14400` |
| `CustomField` | String | Metadata on the call record (max 128 chars) |
| `StatusCallback` | String | Webhook URL for call status updates |
| `StatusCallbackEvents` | Array | `answered`, `terminal`, and/or `ringing` (leg1). For `multipart/form-data`, send each value as `StatusCallbackEvents[]=<event>` |
| `StreamName` | String | Optional stream label, max 32 chars |

## Example request

```bash
curl -u '<api_key>:<api_token>' -X POST \
  'https://api.in.exotel.com/v1/Accounts/<AccountSid>/Calls/connect' \
  -F 'From=+919876543210' \
  -F 'CallerId=0XXXXXXXXXX' \
  -F 'StreamUrl=wss://your-bot.example.com/media' \
  -F 'StreamType=bidirectional' \
  -F 'Record=true' \
  -F 'StatusCallback=https://your-server.com/callback' \
  -F 'StatusCallbackEvents[]=terminal'
```

## Response

```json
{
  "Call": {
    "Sid": "a1b2c3d4e5f6...",
    "Status": "in-progress",
    "From": "+919876543210",
    "PhoneNumberSid": "0XXXXXXXXXX",
    "Direction": "outbound-api",
    "DateCreated": "2025-06-01 10:00:00",
    "RecordingUrl": null
  }
}
```

| Status | Meaning |
|--------|---------|
| `queued` | Call is being prepared |
| `in-progress` | Call is active |
| `completed` | Call ended normally |
| `failed` | Call could not be placed |
| `busy` | Number was busy |
| `no-answer` | Number did not answer |

## After connect

On answer, Exotel opens WSS to `StreamUrl`. Implement the [WebSocket protocol](./websocket-protocol) on your bot. Append `?sample-rate=16000` (or `8000` / `24000`) to the URL when needed.

## Try It

<ApiConsole
  method="POST"
  path="/v1/Accounts/{account_sid}/Calls/connect"
  contentType="form"
  host="api"
  params={[
    { name: 'From', required: true, type: 'string', description: 'Number to dial (E.164)', placeholder: '+919876543210' },
    { name: 'CallerId', required: true, type: 'string', description: 'Your ExoPhone / virtual number', placeholder: '0XXXXXXXXXX' },
    { name: 'StreamUrl', required: true, type: 'string', description: 'Bot WebSocket URL (ws:// or wss://)', placeholder: 'wss://your-bot.example.com/media' },
    { name: 'StreamType', required: true, type: 'string', description: 'Set to bidirectional for two-way audio', defaultValue: 'bidirectional', placeholder: 'bidirectional' },
    { name: 'Record', required: false, type: 'boolean', description: 'true to record the call' },
    { name: 'RecordingChannels', required: false, type: 'string', description: 'single or dual', placeholder: 'single' },
    { name: 'TimeLimit', required: false, type: 'number', description: 'Max call duration in seconds (max 14400)', placeholder: '300' },
    { name: 'CustomField', required: false, type: 'string', description: 'Metadata on the call record (max 128 chars)', placeholder: 'campaign_1' },
    { name: 'StatusCallback', required: false, type: 'string', description: 'Webhook URL for call status updates', placeholder: 'https://your-server.com/callback' },
    { name: 'StreamName', required: false, type: 'string', description: 'Optional stream label (max 32 chars)', placeholder: 'outbound-bot' },
  ]}
/>

## Related

- [What to use when](./what-to-use-when)
- [Connect with Flow](./connect-voice-ai-flow)
- [WSS errors & monitoring](./wss-errors-monitoring)
